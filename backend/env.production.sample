# 프로덕션 환경변수 샘플
# 실제 배포 시 이 파일을 .env.production으로 복사하고 값을 채워주세요.
# ⚠️ 실제 값은 Google Cloud Secret Manager에 저장하는 것을 권장합니다.

# 서버 설정
PORT=8080
NODE_ENV=production

# PostgreSQL 설정 (Cloud SQL)
# Cloud SQL Unix Socket 사용 시:
DB_HOST=/cloudsql/YOUR_PROJECT_ID:asia-northeast3:smarttrade-db
# 또는 Private IP 사용 시:
# DB_HOST=10.x.x.x
DB_PORT=5432
DB_NAME=smarttrade_prod
DB_USER=smarttrade_user
DB_PASSWORD=REPLACE_WITH_SECURE_PASSWORD

# Redis 설정 (Memory Store)
REDIS_HOST=10.x.x.x  # Memory Store Private IP
REDIS_PORT=6379
REDIS_PASSWORD=  # Basic tier는 비밀번호 없음
REDIS_DB=0

# 크롤링 설정
CRAWLING_MAX_PAGES=10
CRAWLING_REQUEST_DELAY=3000
CRAWLING_TIMEOUT=60000
ENABLE_CRAWLER_SCHEDULER=true

# OpenAI API (선택사항)
OPENAI_API_KEY=sk-...your-api-key...

# CORS 설정
CORS_ORIGIN=https://your-frontend-url.run.app

# API URL
API_URL=https://your-backend-url.run.app

